apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: mongodb-pvc
    namespace: flipkart
spec:
    accessModes:
      - ReadWriteOnce
    storageClassName: ebs-sc
    resources:
      requests:
        storage: 20Gi
